// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"tribemedia.io/m/ent/customhostname"
	"tribemedia.io/m/ent/project"
)

// CustomHostname is the model entity for the CustomHostname schema.
type CustomHostname struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// The ID of the project this custom hostname belongs to
	ProjectID uuid.UUID `json:"project_id,omitempty"`
	// The custom hostname
	Hostname string `json:"hostname,omitempty"`
	// The SSL status of the custom hostname
	SslStatus string `json:"ssl_status,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CustomHostnameQuery when eager-loading is set.
	Edges        CustomHostnameEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CustomHostnameEdges holds the relations/edges for other nodes in the graph.
type CustomHostnameEdges struct {
	// Project holds the value of the project edge.
	Project *Project `json:"project,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ProjectOrErr returns the Project value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e CustomHostnameEdges) ProjectOrErr() (*Project, error) {
	if e.Project != nil {
		return e.Project, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: project.Label}
	}
	return nil, &NotLoadedError{edge: "project"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CustomHostname) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case customhostname.FieldHostname, customhostname.FieldSslStatus:
			values[i] = new(sql.NullString)
		case customhostname.FieldID, customhostname.FieldProjectID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CustomHostname fields.
func (ch *CustomHostname) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case customhostname.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				ch.ID = *value
			}
		case customhostname.FieldProjectID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field project_id", values[i])
			} else if value != nil {
				ch.ProjectID = *value
			}
		case customhostname.FieldHostname:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field hostname", values[i])
			} else if value.Valid {
				ch.Hostname = value.String
			}
		case customhostname.FieldSslStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field ssl_status", values[i])
			} else if value.Valid {
				ch.SslStatus = value.String
			}
		default:
			ch.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CustomHostname.
// This includes values selected through modifiers, order, etc.
func (ch *CustomHostname) Value(name string) (ent.Value, error) {
	return ch.selectValues.Get(name)
}

// QueryProject queries the "project" edge of the CustomHostname entity.
func (ch *CustomHostname) QueryProject() *ProjectQuery {
	return NewCustomHostnameClient(ch.config).QueryProject(ch)
}

// Update returns a builder for updating this CustomHostname.
// Note that you need to call CustomHostname.Unwrap() before calling this method if this CustomHostname
// was returned from a transaction, and the transaction was committed or rolled back.
func (ch *CustomHostname) Update() *CustomHostnameUpdateOne {
	return NewCustomHostnameClient(ch.config).UpdateOne(ch)
}

// Unwrap unwraps the CustomHostname entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ch *CustomHostname) Unwrap() *CustomHostname {
	_tx, ok := ch.config.driver.(*txDriver)
	if !ok {
		panic("ent: CustomHostname is not a transactional entity")
	}
	ch.config.driver = _tx.drv
	return ch
}

// String implements the fmt.Stringer.
func (ch *CustomHostname) String() string {
	var builder strings.Builder
	builder.WriteString("CustomHostname(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ch.ID))
	builder.WriteString("project_id=")
	builder.WriteString(fmt.Sprintf("%v", ch.ProjectID))
	builder.WriteString(", ")
	builder.WriteString("hostname=")
	builder.WriteString(ch.Hostname)
	builder.WriteString(", ")
	builder.WriteString("ssl_status=")
	builder.WriteString(ch.SslStatus)
	builder.WriteByte(')')
	return builder.String()
}

// CustomHostnames is a parsable slice of CustomHostname.
type CustomHostnames []*CustomHostname
